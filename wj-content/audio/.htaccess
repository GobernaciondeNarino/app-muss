# Musa Café · Sin ejecución de PHP en las carpetas de subidas y audio
<FilesMatch "\.(php|phtml|phar|cgi|pl)$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
  </IfModule>
</FilesMatch>
Options -Indexes
